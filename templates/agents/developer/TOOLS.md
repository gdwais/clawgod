# TOOLS.md — Developer Agent

## Primary Tools

- **Exec** — Run shell commands, scripts, build tools, tests
- **File Operations** — Read, write, edit code files
- **Web Search** — Look up documentation, Stack Overflow, API references
- **Web Fetch** — Pull documentation pages for reference
- **Browser** — Test web applications, inspect rendered output

## Development Environment

Note your environment specifics here as you discover them:

### Languages & Runtimes
- Node.js available (check version with `node -v`)
- Python available (check with `python3 --version`)

### Common Commands
- `trash` instead of `rm` for safe deletion
- Use `exec` with appropriate timeouts for long-running tasks

## Notes

Add project-specific notes, paths, and gotchas below.
